# Bằng chứng nghiệm thu — P01

> Ghi theo [quy tắc chung](../README.md#7-bằng-chứng-nghiệm-thu).
> Tip docs / full QA: `526119e` · Ngày: `2026-09-15` · Người chạy: Minh Truong (executor) + Revie (full QA PASS_WITH_NOTES)
> Coding tip Task 16 (trước Task 18): `20ff22a`

Tip docs P01 (Task 18 evidence + fresh-clone guide): `526119e` · Branch: `phase01/foundation`.
Tip coding slice P01 (Task 1–16, Task 17 OUT): `20ff22a`.

| Tip SHA | Nội dung |
|---|---|
| `5c7aad4` | feat(api): scaffold workspace and typed config |
| `4f3ca77` | feat(api): RFC 9457 problem responses and shared error codes |
| `568ab0f` | feat(api): chi router, healthz and privacy-safe access log |
| `b7476e6` | feat(api): pgx pool and readyz with database dependency check |
| `600e239` | feat(api): prometheus metrics, structured logger and graceful shutdown |
| `cc6e7bb` | feat(api): base migration for extensions and migrate command |
| `e028d33` | feat(api): sqlc pipeline and database preflight on boot |
| `2d43317` | fix(api): regenerate sqlc models after river migrations |
| `0f3bbe5` | feat(api): river worker lifecycle with noop job |
| `6707f52` | feat(api): openapi 3.1 contract and generated typed client |
| `70a21d8` | feat(web): scaffold next app and port design tokens from mockups |
| `b9b9402` | feat(web): theme provider, self-hosted fonts and next-intl routing |
| `8d59d9a` | feat(web): ui primitives ported from mockup tokens |
| `00a6577` | feat(web): public demo fixtures and in-browser search |
| `789f0b8` | feat(web): landing hero demo and json table from mockup |
| `4d699df` | feat(web): private app shell with rail, vault pill and error states |
| `20ff22a` | feat(infra): compose stack, pinned images, csp headers and smoke script |
| `526119e` | docs: fresh-clone guide and P01 acceptance evidence (Revie full QA tip) |

| ID | Ca | Bằng chứng | Kết quả |
|---|---|---|---|
| P01-A1 | Fresh checkout + env mẫu | README có mục **Chạy tại máy** (clone `https://github.com/zone17th/engram`, `pnpm install`, `pnpm stack:up` / `make dev`, `pnpm smoke`). **Chưa** chạy được fresh-clone `stack:up` + smoke trên máy Revie lẫn máy executor: Docker có nhưng **thiếu plugin Compose** (`docker: 'compose' is not a docker command`). | partial — hướng dẫn có; live compose smoke chưa verify |
| P01-A2 | Ready dependency | Ca tay theo plan (readyz 503 khi stop db / healthz 200 / readyz 200 sau start) **chưa** chạy live vì không lên được compose stack. Code + test unit `internal/httpx` (ready/health) có trên tip `b7476e6`/`20ff22a`. | not fully verified (compose blocked) |
| P01-A3 | Proxy/routing | `scripts/smoke.sh` + rewrite/CSP trên tip `20ff22a`; Vitest `apps/web/tests/routing.test.ts` + `csp.test.ts` pass local (2026-09-15). Playwright proxy e2e **OUT** (Task 17 skipped). Live `pnpm smoke` qua origin :3000 **chưa** chạy. | partial — unit/routing OK; live smoke OUT |
| P01-A4 | Visual | Task 17 (Playwright + 18 baseline screenshot) **OUT** — không có `.github/workflows/*`, không có `apps/web/e2e/`. Đối chiếu mockup dựa trên port code Tasks 10–15 (`4d699df` tip shell). Overflow/`toHaveScreenshot` chưa có CI e2e. | deferred — chờ Revie full QA / Task 17 hoặc QA tay |
| P01-A5 | Demo boundary | Vitest: `demo-search.test.ts`, `hero-demo.test.tsx`, `csp.test.ts` (ranh giới demo / không claim encrypt) — **pass** local 2026-09-15 trên tip `20ff22a`. Playwright “không gửi request ra ngoài” **OUT** (Task 17). | pass (unit); e2e OUT |
| P01-A6 | Keyboard/i18n | Vitest: `hero-demo.test.tsx` (ArrowDown/Enter/Escape, reduced motion), `i18n.parity.test.ts`, logo `engram` trong shell/landing tests — **pass** local. Playwright axe/logo **OUT**. | pass (unit); e2e OUT |
| P01-A7 | CI | Task 17 OUT — không có workflow `api.yml` / `web.yml` / `e2e.yml`. Thay bằng lệnh local: `cd apps/api && go test ./... -short` (pass, Go 1.25.0 via GOTOOLCHAIN) và `corepack pnpm@10.34.5 -r test` (shared-types 9 + web 95 tests pass). Full `go test ./...` (testcontainers) **chưa** chạy (cần Docker Compose/testcontainers). | partial — local checks pass; CI workflows OUT |

## Lệnh đã chạy (local, 2026-09-15 UTC+7)

```bash
# tip 20ff22a trên worktree phase01-foundation
cd apps/api && GOTOOLCHAIN=auto go test ./... -short
# → ok config/db/httpx/obs/worker/cmd/api

corepack pnpm@10.34.5 -r test
# → packages/shared-types: 9 passed
# → apps/web: 95 passed (10 files)
```

**Không chạy:** `pnpm stack:up`, `pnpm smoke`, `make dev` full stack, `go test ./...` (không `-short`), Playwright, fresh clone `/tmp`.

## NOTES

- **CSP `unsafe-inline` (P01 intentional):** `script-src` / `style-src` còn `'unsafe-inline'`, chưa nonce — exception có hạn để giữ landing SSG (TTFB). Revie Task 16: PASS_WITH_NOTES; khoá bằng `apps/web/tests/csp.test.ts`. P06 đóng theo SPEC §10.2.
- **Compose smoke chưa live-verify:** máy Revie và máy executor đều thiếu Docker Compose plugin; tip `20ff22a` có `infra/docker-compose.yml`, `scripts/smoke.sh`, `pnpm stack:up` nhưng **chưa** chứng minh `OK — smoke pass` trên môi trường thật.
- **Go toolchain:** plan ghi Go 1.27; `apps/api/go.mod` khai báo `go 1.25.0`; image pin `GO_BUILD_IMAGE` hướng golang 1.27. Local verify dùng `go1.25.0` (GOTOOLCHAIN=auto từ host 1.24.4).
- **Task 17 OUT:** không thêm `.github/workflows/*`; không Playwright baseline; P01-A4/A7 e2e/CI đánh dấu deferred/partial.
- **sqlc rework tip:** `2d43317` — regenerate sqlc models sau river migrations (sau `e028d33`).
- Module path Go/npm vẫn còn chuỗi lịch sử `save-all-by-keyword` ở một số chỗ; repo GitHub thật: `https://github.com/zone17th/engram`.

## Nợ kỹ thuật chuyển sang phase sau

- **CSP chưa đạt SPEC §10.2**: `script-src`/`style-src` còn `'unsafe-inline'`, chưa có nonce, vì nonce ép landing sang dynamic rendering còn landing phải là SSG (TTFB < 200 ms). P06 chọn một trong hai: nonce + landing dynamic-có-cache, hoặc CSP hash-based giữ SSG.
- `VaultPill` ở `/app` nhận state cứng `locked` từ shell — P03 nối vào state vault thật.
- `/app` hiển thị `DEMO_ITEMS`; P04 thay bằng dữ liệu thật và xoá import fixture.
- Job `noop` của River chỉ để chứng minh lifecycle — P05 thay bằng job embedding.
- Primitive UI viết tay, chưa có Radix. P02–P03 thay `Switch`/`Segmented` bằng `@radix-ui/react-switch` / `react-toggle-group` và thêm `Dialog`/`Popover`/`DropdownMenu`, giữ nguyên class từ `tokens.css`.
- Landing dùng `landing.privacyBody1` ở thì tương lai và có `landing.previewNote`: P01 chưa có crypto. P03 đổi lại đúng copy mockup và bỏ `previewNote` khi vault thật đã bật.
- Chưa có `turbo.json`: workspace mới có một package Node. Thêm khi số package Node vượt hai (sớm nhất P04).
- `/app` khoá cứng locale bằng hằng `APP_LOCALE = 'vi'` (dùng cho cả `<html lang>` lẫn `NextIntlClientProvider`). P01 chưa có session nên chưa có hồ sơ để đọc locale. P02 thay hằng này bằng locale của người dùng và bỏ file `i18n/app-locale.ts`.
- Live compose smoke + Task 17 CI/e2e còn mở (NOTES mang sang backlog; Revie full QA tip `526119e` = PASS_WITH_NOTES, không chặn đóng P01).

## Ảnh chụp

Không đưa ảnh chứa recovery key, passphrase hay dữ liệu thật của người dùng vào đây.
Mọi ảnh của P01 chỉ chứa fixture công khai trong `apps/web/lib/demo-fixtures.ts`.
Task 17 OUT nên chưa đính kèm baseline Playwright.
